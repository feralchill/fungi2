---
import MessageBar from '../components/MessageBar.astro';
import Navigation from '../components/Navigation.astro';
import Banner from '../components/Banner.astro';
import BottomTab from '../components/BottomTab.astro';

interface Props {
  title: string;
  currentPage?: string;
}

const { title, currentPage = '/' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Coaching, workshops and consultation for individuals and organisations ready to discover the power of functional mushrooms." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/favicon-96x96.png?v=2" sizes="96x96" />
    <link rel="shortcut icon" href="/favicon.ico?v=2" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png?v=2" />
    <meta name="apple-mobile-web-app-title" content="The Fungi Consultant" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- Open Graph / Social Media Preview -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content="Coaching, workshops and consultation for individuals and organisations ready to discover the power of functional mushrooms." />
    <meta property="og:image" content="https://raw.githubusercontent.com/feralchill/fungi/main/IMG_6019.png" />
    <meta property="og:url" content="https://thefungiconsultant.com" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content="Coaching, workshops and consultation for individuals and organisations ready to discover the power of functional mushrooms." />
    <meta name="twitter:image" content="https://raw.githubusercontent.com/feralchill/fungi/main/IMG_6019.png" />

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Literata:wght@400;500;600&family=Abril+Fatface&family=Almendra+Display&display=swap" rel="stylesheet">
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <MessageBar />
    <Navigation currentPage={currentPage} />
    <Banner />
    <main>
      <slot />
    </main>
    <BottomTab />
  </body>
</html>

<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
    -webkit-tap-highlight-color: transparent;
  }

  html {
    font-family: 'Literata', serif;
    background: #EFEFED;
    overflow-x: hidden;
    -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  }

  body {
    min-height: 100vh;
    padding-top: 90px;
    overflow-x: hidden;
    max-width: 100%;
    -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  }

  button, a {
    -webkit-tap-highlight-color: rgba(0, 0, 0, 0) !important;
    -webkit-tap-highlight-color: transparent !important;
  }

  img {
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
    pointer-events: none;
    -webkit-user-drag: none;
    -webkit-touch-callout: none;
  }

  main {
    max-width: 100%;
    overflow-x: hidden;
    padding: 0 20px 100px;
  }

  @media (max-width: 1024px) {
    body {
      padding-top: 0;
    }

    main {
      padding: 0 15px 100px;
    }
  }

  /* Landscape mobile/tablet - prevent font size inflation */
  @media (min-width: 768px) and (max-width: 1024px) and (orientation: landscape),
         (min-width: 768px) and (max-width: 1024px) and (max-height: 500px) {
    html {
      font-size: 14px;
    }
  }
</style>
